<%= form_for(@my_resource) do |f| %>
  <% if @my_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_resource.errors.count, "error") %> prohibited this my_resource from being saved:</h2>

      <ul>
      <% @my_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :fields %><br>
    <%= f.text_area :fields %>
  </div>
  <div class="field">
    <%= f.label :my_application_id %><br>
    <%= f.text_field :my_application_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
